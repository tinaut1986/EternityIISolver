# =========================
# Docker Compose Environment
# =========================
# Copy this file to .env and fill in your values:
#   cp .env.template .env

# Server Configuration
SERVER_PORT=5210
ASPNET_ENV=Development

# Database Configuration
DB_SERVER=your_db_host          # e.g., mariadb_server, localhost, 192.168.1.100
DB_NAME=eternity_db
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Docker Network (external network where the DB container is running)
# This must match an existing Docker network name. Examples:
#   - shared_database_network (if your DB is in another compose project)
#   - bridge (Docker's default network)
#   - my_custom_network
DOCKER_NETWORK=your_network_name
